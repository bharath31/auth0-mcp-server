# Auth0 MCP Server Configuration
# Most settings can be pulled automatically from Auth0 CLI

# -------------------------------------------------------------------------------
# Mode Settings
# -------------------------------------------------------------------------------
# Set to 'true' to enable debug mode (prefers local CLI path)
# AUTH0_MCP_DEBUG=true

# -------------------------------------------------------------------------------
# Auth0 Settings
# -------------------------------------------------------------------------------
# These settings are typically pulled from Auth0 CLI automatically
# Only set these if you want to override CLI settings

# AUTH0_DOMAIN=your-tenant.auth0.com
# AUTH0_TOKEN=your-auth0-management-api-token

# -------------------------------------------------------------------------------
# Auth0 CLI Settings
# -------------------------------------------------------------------------------
# Path to Auth0 CLI executable
# In debug mode, prefer local CLI path
# In production mode, prefer global CLI in PATH
# AUTH0_CLI_PATH=/path/to/auth0-cli/auth0

# -------------------------------------------------------------------------------
# Server Settings
# -------------------------------------------------------------------------------
# Server port (default: 3000)
# PORT=3000

# Debug logging (uncomment to enable)
# DEBUG=auth0-mcp:* 